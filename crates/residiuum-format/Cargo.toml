[package]
name = "residiuum-format"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "MIT"
authors.workspace = true
description = "Residiuum survival wire format: frame codec, integrity, and salvage scanning (Stage 2)."
readme = "README.md"
repository.workspace = true
keywords = ["residiuum", "wire-format", "salvage", "blake3", "crc32c"]
categories = ["encoding", "data-structures"]
include = ["Cargo.toml", "README.md", "src/**", "tests/**"]

[dependencies]
blake3.workspace = true
crc32c.workspace = true
residiuum-atomics = { path = "../residiuum-atomics" }
thiserror.workspace = true

[dev-dependencies]
# DEF-091 — property tests for untrusted frame / envelope / scan surfaces.
proptest = "1.6"
# CSQ-3 frozen corpus manifest parsing.
serde_json.workspace = true