[package]
name = "residiuum-atomics"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license = "MIT"
description = "Residiuum Atomic protocol types: identity, limits, vocabulary, outcomes, and formal lifecycle states."
readme = "README.md"
keywords = ["residiuum", "atomics", "protocol"]
categories = ["data-structures"]
include = ["Cargo.toml", "README.md", "src/**", "tests/**", "examples/**", "spec/**"]

[dependencies]
blake3.workspace = true
getrandom.workspace = true
num-bigint.workspace = true
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true
trybuild = "1.0"

[lints.rust]
unexpected_cfgs = { level = "warn" }

# Pure protocol crate. No store, SDK, format, heap, file, network, or thread runtime.
